{
  "name": "speedcheck-frontend",
  "version": "1.05.0",
  "description": "Frontend static file server with PWA support",
  "scripts": {
    "start": "node build-version.js && node server.js",
    "build:version": "node build-version.js",
    "prebuild": "npm run build:version"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {}
}